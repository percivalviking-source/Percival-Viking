async function carregarParcial(alvo){const arquivo=alvo.dataset.partial;if(!arquivo)return;try{const r=await fetch(arquivo);if(!r.ok)throw new Error(`Falha ao carregar ${arquivo}`);alvo.innerHTML=await r.text()}catch(e){alvo.innerHTML=`<div class="error">Erro ao carregar ${arquivo}</div>`;console.error(e)}}async function iniciarPercivalViking(){const parciais=[...document.querySelectorAll("[data-partial]")];for(const parcial of parciais){await carregarParcial(parcial)}if(typeof carregarFeedHome==="function")carregarFeedHome()}document.addEventListener("DOMContentLoaded",iniciarPercivalViking);
