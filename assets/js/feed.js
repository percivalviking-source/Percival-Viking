function dataBR(d){return new Date(`${d}T12:00:00`).toLocaleDateString("pt-BR",{day:"2-digit",month:"2-digit",year:"numeric"})}function meta(a){return `<div class="meta-row"><span>${a.fonte||"Percival Viking"}</span><span class="dot"></span><time datetime="${a.data}">${dataBR(a.data)}</time></div>`}function hero(a){return `<article class="hero-card"><a href="${a.url}"><img class="hero-cover" src="${a.capa}" alt="${a.alt||a.titulo}" loading="eager"></a><div class="hero-body"><span class="category">${a.categoria}</span><a href="${a.url}"><h2 class="hero-title">${a.titulo}</h2></a><p class="excerpt">${a.resumo}</p><div class="share-row">${meta(a)}<button class="share-button" type="button" data-share="${a.url}" data-title="${a.titulo}">Compartilhar</button></div></div></article>`}function card(a){return `<article class="news-card"><a href="${a.url}"><img class="news-cover" src="${a.capa}" alt="${a.alt||a.titulo}" loading="lazy"><div class="news-body"><span class="category">${a.categoria}</span><h3 class="news-title">${a.titulo}</h3><p class="excerpt">${a.resumo}</p>${meta(a)}</div></a></article>`}function compact(a){return `<article class="compact-card"><a href="${a.url}"><img class="compact-cover" src="${a.capa}" alt="${a.alt||a.titulo}" loading="lazy"><div class="compact-body"><span class="category">${a.categoria}</span><h3 class="compact-title">${a.titulo}</h3>${meta(a)}</div></a></article>`}async function compartilhar(url,titulo){const link=new URL(url,location.origin).href;if(navigator.share){await navigator.share({title:titulo,url:link});return}await navigator.clipboard.writeText(link);alert("Link copiado.")}function ativarShare(){document.querySelectorAll("[data-share]").forEach(b=>b.addEventListener("click",()=>compartilhar(b.dataset.share,b.dataset.title)))}async function carregarFeedHome(){const destaque=document.querySelector("#destaque-principal"),alta=document.querySelector("#feed-em-alta"),recentes=document.querySelector("#feed-recentes");if(!destaque||!alta||!recentes)return;destaque.innerHTML='<div class="loading">Carregando destaque...</div>';alta.innerHTML='<div class="loading">Carregando artigos...</div>';recentes.innerHTML='<div class="loading">Carregando recentes...</div>';try{const r=await fetch("/data/artigos.json");if(!r.ok)throw new Error("Falha ao carregar artigos.json");const artigos=(await r.json()).sort((a,b)=>new Date(b.data)-new Date(a.data));destaque.innerHTML=artigos[0]?hero(artigos[0]):"";alta.innerHTML=artigos.slice(1,4).map(card).join("");recentes.innerHTML=artigos.slice(4,10).map(compact).join("");ativarShare()}catch(e){console.error(e);destaque.innerHTML='<div class="error">Não consegui carregar os artigos agora.</div>';alta.innerHTML="";recentes.innerHTML=""}}
